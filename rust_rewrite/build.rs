// use dotenv::dotenv;
// use std::env;

fn main() {
    // dotenv().ok();
    // if let Ok(database_url) = env::var("DATABASE_URL") {
    //     println!("cargo:rustc-env=DATABASE_URL={}", database_url);
    // }
}